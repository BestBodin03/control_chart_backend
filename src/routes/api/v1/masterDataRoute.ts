import { Router } from 'express';
// import { getMasterData } from '../../../controllers/MasterDataController';
// import { getMasterData, addAllCollections } from '../../../controllers/MasterDataController';

const router = Router();


// router.get('/master-data', getMasterData);
// router.post('/master-data/add-all-data', addAllCollections);


export default router;
// // POST /customer-products
// router.post('/customer-products', (req, res) => controller.createCustomerProduct(req, res));