export type CapabilityProcess = {
  std: number;   // Standard Deviation
  cpu: number;   // Capability Upper
  cpl: number;   // Capability Lower
  cp: number;    // Process Capability
  cpk: number;   // Process Capability Index
};
